Hello, World! 

This template will contain an error. The error will be a Notice due to an unset variable.

The variable that isn't set is {{$unsetVariable}}.

This should produce a TemplumTemplateError, which you can catch. You can then display error information about the error.
